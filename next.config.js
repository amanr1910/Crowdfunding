/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}
// next.config.js
module.exports = {
  images: {
    domains: ['crowdfunding.infura-ipfs.io'],
  },
}

// module.exports = nextConfig
